import Interval from "./Interval";

const About = () => (
  <div>
    <h1>About</h1>
    <p>This is an about page that has an useEffect example inside!</p>
    <p>Check it! (remember the open the console)</p>
    <Interval />
  </div>
);

export default About;
